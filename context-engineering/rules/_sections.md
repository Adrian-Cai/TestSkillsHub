# 规则索引

本目录包含上下文工程 skill 的所有规则文件，执行前请按需加载：

| 规则文件 | 说明 | 何时加载 |
| --- | --- | --- |
| `context-hierarchy.md` | 上下文层次结构（规则文件、规范、源文件、错误输出、对话历史） | 设置 agent 上下文时读 |
| `context-packing.md` | 上下文打包策略（脑力倾倒、选择性包含、层次摘要） | 组织 agent 输入时读 |
| `confusion-management.md` | 困惑管理（上下文冲突、需求不完整、内联规划） | 遇到歧义时读 |
| `anti-patterns.md` | 上下文工程反模式 | 审查上下文质量时读 |
