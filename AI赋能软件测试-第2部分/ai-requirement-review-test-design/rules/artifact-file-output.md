# 产物输出规则

## 文件落地
- 默认写入：`outputs/reviews/<slug>.md`
- `<slug>` 使用英文小写、数字和连字符，例如：`coupon-requirement-review.md`
- 如果用户指定路径，以用户路径为准。

## 最终回复
最终回复只需包含：
- 已完成的核心内容摘要
- 产物文件路径
- 如有缺失信息，说明已放入“待确认项”

## 不允许
- 不要只在对话中展示完整结果而不写文件。
- 不要把未确认信息写成确定规则。
- 不要输出与需求评审无关的泛 AI 概念说明。
