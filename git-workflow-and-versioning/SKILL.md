---
name: Git工作流与版本管理
description: 结构化 git 工作流实践。适用于提交、分支、解决冲突或需要跨多个并行流组织工作时。
---

# Git工作流与版本管理

## 概述

Git 是你的安全网。将提交视为保存点，分支视为沙箱，历史视为文档。随着 AI agent 高速生成代码，纪律严明的版本控制是保持变更可管理、可审查和可回滚的机制。

## 何时使用

始终。每个代码变更都通过 git。

## 规则索引

本 skill 包含以下规则文件，按需加载：

| 规则文件 | 说明 | 何时加载 |
| --- | --- | --- |
| `rules/commit-practices.md` | 提交实践（原子提交、描述性消息、关注点分离、变更大小） | 进行代码提交时读 |
| `rules/branching-strategy.md` | 分支策略（特性分支、分支命名、工作树） | 创建或管理分支时读 |
| `rules/pre-commit-hygiene.md` | 提交前卫生（检查清单、git hooks、生成文件处理） | 提交代码前读 |
| `rules/git-debugging.md` | 使用 Git 进行调试（二分法、日志搜索、blame） | 调试问题时读 |

## 常见误解

| 误解 | 现实 |
|------|------|
| "功能完成时我会提交" | 一个巨大提交无法审查、调试或回滚。提交每个切片。 |
| "消息不重要" | 消息是文档。未来的你（和未来的 agent）将需要理解更改了什么以及为什么。 |
| "我稍后会压缩所有内容" | 压缩会破坏开发叙述。从一开始优先使用干净的增量提交。 |
| "分支增加开销" | 短暂分支是免费的，防止冲突工作碰撞。长期分支才是问题——在 1-3 天内合并。 |
| "我稍后会拆分这个更改" | 大变更更难审查、部署风险更高、更难回滚。在提交前拆分，而不是之后。 |
| "我不需要 .gitignore" | 直到包含生产机密的 `.env` 被提交。立即设置它。 |

## 红旗

- 累积的大未提交更改
- 提交消息如 "fix"、"update"、"misc"
- 格式更改与行为更改混合
- 项目中没有 `.gitignore`
- 提交 `node_modules/`、`.env` 或构建工件
- 与 main 显著分歧的长期分支
- 强制推送到共享分支

## 验证

对于每次提交：

- [ ] 提交做一件逻辑事情
- [ ] 消息解释为什么，遵循类型约定
- [ ] 提交前测试通过
- [ ] 差异中没有机密
- [ ] 没有仅格式更改与行为更改混合
- [ ] `.gitignore` 覆盖标准排除
