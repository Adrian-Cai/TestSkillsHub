# 规则索引

本目录包含 ai-testcase-design skill 的规则文件，执行时按以下顺序加载。

| 规则文件 | 说明 | 何时加载 |
| --- | --- | --- |
| `artifact-file-output.md` | 产物文件输出规范 | 每次执行都要读 |
| `business-rule-analysis.md` | 业务规则拆解规范 | 每次执行都要读 |
| `test-design-methods.md` | 测试设计方法生成规范 | 每次执行都要读 |
| `omission-review-rules.md` | 用例遗漏检查规范 | 每次执行都要读 |
| `testcase-refine-priority.md` | 裁剪、合并与优先级规则 | 每次执行都要读 |

