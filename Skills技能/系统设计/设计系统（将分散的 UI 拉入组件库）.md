任务：将本项目中分散的 UI 组件、样式令牌和重复交互抽象为一个统一的设计系统，即一个可重用且可维护的组件库。用库组件替换手工制作的一次性组件，将所有新 UI 路由通过该库，不允许绕过，并在最后添加审查技能。这是一次重构，因此从工作树开始。

对于此任务，为自己编写一个新的端到端 /目标：完成整个计划，而不仅仅是下一步，直到架构、实现、测试、审查和最终结果达到标准。将该目标拆分为独立的部分，生成尽可能多的并行代理以更好地、更快地完成，并为每个代理提供其专属的 /目标，包括其预期交付物、验证和完成标准。

并发调度它们，在正确的地方持续跟踪进度，随着结果返回进行综合，解决冲突，继续实施，在重要步骤后运行实时验证，并以审查、适当的提交/提交以及最终总结结束。验证应涵盖真实的端到端路径，包括浏览器/计算机使用、点击、键盘操作以及任何必要操作。除非因缺少凭据、破坏性歧义或冲突要求而受阻，否则不要在部分进度后停止