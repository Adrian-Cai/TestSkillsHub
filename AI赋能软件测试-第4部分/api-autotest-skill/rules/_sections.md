# 规则索引

本目录包含 api-autotest-skill 的规则文件，执行时按以下顺序加载。

| 规则文件 | 说明 | 何时加载 |
| --- | --- | --- |
| `api-info-extraction.md` | 接口资料整理规范 | 每次执行都要读 |
| `test-element-decomposition.md` | 测试要素拆解规范 | 每次执行都要读 |
| `test-point-matrix.md` | 测试点矩阵生成规范 | 每次执行都要读 |
| `case-data-design.md` | 参数化用例数据设计规范 | 每次执行都要读 |
| `pytest-script-rules.md` | pytest + requests 脚本生成规范 | 每次执行都要读 |
| `manual-review-checklist.md` | 人工审查清单输出规范 | 每次执行都要读 |
